{
  "name": "poketypes",
  "version": "0.1.0",
  "private": true,
  "description": "Single-file Pokémon Types PWA",
  "scripts": {
    "dev": "live-server docs --port=8000 --open=./index.html",
    "start": "live-server docs --port=8000 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
