{
  "name": "poketypes",
  "version": "0.1.0",
  "private": true,
  "description": "Single-file Pokémon Types PWA",
  "scripts": {
    "dev": "live-server docs --port=8000 --open=./index.html",
    "start": "live-server docs --port=8000 --open=./index.html",
    "build-icons": "node scripts/build-icons.js",
    "update-manifest": "node scripts/update-manifest.js",
    "build": "npm run build-icons && npm run update-manifest"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sharp": "^0.34.5"
  }
}
